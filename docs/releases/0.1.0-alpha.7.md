# JevBrowse 0.1.0-alpha.7 (private portable alpha, candidate)

**Status: candidate, FROZEN at the exact ZIP identified below. No public release until the security review and the final-artifact acceptance results have
both been assessed.** Two things gate it: [CLEAN_WINDOWS_CHECKLIST.md](../CLEAN_WINDOWS_CHECKLIST.md) has not been run on this exact ZIP by anyone other
than the author, and the shortcut-token security fix (below) has not had independent review — passing the interception regression (row 25) establishes
that **one** specific attack is blocked; it does not establish that the token boundary as a whole is secure. A separate, reproducible,
**root-cause-unresolved** failure in the packaged smoke's engine checks is also open — see "Packaged smoke" below. The clean-Windows checklist now asks a
tester to confirm the individual Private-session behaviors that failure left unverified by hand; **those manual results are evidence for each behavior,
not an explanation or a resolution of the engine-check failure itself, which stays open regardless of how they come out.**
This is the first alpha meant to be *used every day*, not just tested. alpha.6 stays as the minimal-change option (same safety fixes, none of the features below).

**Full rebuild history for alpha.7** (every rebuild since it was first cut, corrected from an earlier version of this document that wrongly called the
current rebuild "the only one"):

| # | Source commit | ZIP SHA-256 | Why |
|---|---|---|---|
| 1 | `629846d` | `bfeb1f5ef34f0db70c3d93b001237624363c888f4ff07e2f41d404fcd30d0e84` | First alpha.7 cut (the full feature set below) |
| 2 | `63e8ae2` | `309bc884d41597c3e113ab2d38546d8e138c7f9c0d5c59c995e238d8de3fa76d` | Zoom fix (the engine was zooming a second time on top of the app's own) |
| 3 | `ef7f7f2` | `05990575e9d168f81895e947e7a6e0e0bc1f54c39bc2675a132d7e0fb90f3862` | Shortcut-forwarding, stale address bar, and iframe/wheel zoom fixes |
| 4 | `069cd84` | `f46bd00ba5af341c3748120a611becb8743ab38965f9323dfabc581169b0b60a` | The shortcut/zoom page-message token (security fix) and the lease-ordering bug it introduced |
| 5 | `4520ec2` | `d6a791fe7a898f1afe91b3cb175693a47986296bc235796548f930031989e65f` | **The ZIP under test.** The postMessage-interception fix below |

`HEAD` has since moved one commit past `4520ec2`, adding the diagnostic logging described under "Packaged smoke" below (a `try`/`catch`-wrapped, append-only
log write inside the `--additions-check` bench method only — no shipped, user-facing behaviour changed). That did not trigger a sixth rebuild: it is not
a fix, security-relevant, or reachable outside that one diagnostic bench mode, so the ZIP under test (#5 above) still reflects everything that matters
for acceptance testing. If a real fix for the packaged-smoke failure is found later, that becomes rebuild #6 and this table gains a row.

| | |
|---|---|
| Source commit (what this ZIP was built from) | `4520ec2a74a9b74d481fb34cbdd4fe9edb3a006c` |
| File | `JevBrowse-0.1.0-alpha.7-win-x64.zip` (90 MB, self-contained, unsigned) |
| **SHA-256 (full)** | `d6a791fe7a898f1afe91b3cb175693a47986296bc235796548f930031989e65f` |
| Symbols (keep private) | `JevBrowse-0.1.0-alpha.7-win-x64-symbols.zip`, SHA-256 (full) `e46203965f09c74c6f480c14a17ecf1ba07a3cc18a3ff9f62b6f8a8f4a43fe0b` |
| Packaged smoke | **FAIL, 19/20** ([json](0.1.0-alpha.7-packaged-smoke.json)) — one reproducible, root-cause-unresolved failure; see below. recovery (36), protection (19), history (13), links (8) all clean; additions/engine checks did not complete (8 of 31 steps ran before the abort) |
| Unit tests | 594 across 9 projects, all passing |

## New for people
- **Bookmarks**: Ctrl+D adds or removes; Ctrl+Shift+O opens a searchable list; **import** the HTML file exported by Chrome, Edge, Firefox or Brave (web addresses only). Private sessions cannot add bookmarks.
- **Search engine choice**: DuckDuckGo (default), Google, Bing, Brave Search, Startpage, Ecosia (More menu, Bookmarks and search engine).
- **History** (Ctrl+H): search, Forget one entry, Clear all. Never records Private or agent sessions or pages classed Sensitive/Secret (banking, health).
- **Downloads list** (Ctrl+J): newest first, finished or not, **Show in folder**. Files are never opened from the list. A Private-session download is listed in memory only.
- **Per-site zoom** (Ctrl+ / Ctrl- / Ctrl+0): the familiar 25%..500% ladder, remembered by site, not remembered in Private/agent sessions or on Sensitive pages.
- **Links from other apps**: a link clicked elsewhere opens in the running JevBrowse (a new tab in the default workspace, never in a Private session); only http/https. **Make JevBrowse your default browser** registers it for the current user and opens Windows Settings; Windows requires you to choose it there. *Remove JevBrowse from the browser list* undoes the registration.

## Security fix: shortcuts and zoom persistence required a real key press, not just a message (independent-review finding)
A page -- or any frame embedded in it, including an ad or a widget -- could call `chrome.webview.postMessage('jev:key:closetab')` (or `bookmark`, `addr`,
any recognized shortcut, or `jev:zoom-in`/`-out`/`-reset`) **directly**, with no key press at all, and the browser would act on it: close the active tab
(losing unfinished work), add or remove a bookmark, move keyboard focus into the address bar, or write a zoom level to disk. The `e.isTrusted` check in the
injected script only protected that one listener; nothing stopped a page calling the same browser API itself.

**Fix:** each renderer generates a random, unguessable token when it is created. It is baked into the host-authored script text before that script is
registered with the page -- never placed on `window` or anywhere a page script can read it -- and every `jev:key:*`/`jev:zoom-*` message must carry the
matching token or it is silently ignored, in the top document and in every frame. A page cannot forge the token because it cannot read the closure that
holds it. Informational signals from content (typing happened, a password field exists, media is playing) are unaffected: they only ever narrow when the
browser keeps a tab awake, never act on the person's behalf, so they were not part of this boundary.

**The platform limit, stated plainly:** WinUI 3's managed `WebView2` control does not expose `CoreWebView2Controller.AcceleratorKeyPressed`, the native,
page-independent keyboard hook a fully host-authenticated fix would use; reaching it needs COM interop this pass did not add, to keep the change bounded.
The token is a strong, verifiable boundary (122 bits of entropy the page cannot read), not literally "the OS says a key was pressed" -- disclosed here
rather than overstated. Verified with real WebView2 (row 24, above) and covered on the code side by the ordering-bug regression it exposed.

## Postmessage-interception fix (the gap CHORD_TOKEN_REVIEW.md flagged as untested)
`PageScript` now captures a bound reference to `chrome.webview.postMessage` as the **first** thing it does in each document, before anything else runs
(guaranteed by `AddScriptToExecuteOnDocumentCreatedAsync`), and every outgoing message uses that captured reference from then on. A page script that runs
afterward and reassigns `chrome.webview.postMessage` to a function of its own no longer sees any of this app's own calls, including genuine, real-key-press
ones -- so it cannot read a token off a real forwarded message and replay it as something more damaging. This was the specific, named, unverified gap in
[docs/security/CHORD_TOKEN_REVIEW.md](../security/CHORD_TOKEN_REVIEW.md); that document is updated alongside this record.

**New negative regression (interaction-pass row 25), mutation-checked, including a genuinely cross-origin frame:** a page and an embedded frame -- on a
different port, a different origin under Chromium's same-origin policy, not the same local server as the page -- each install an interceptor, a real
Ctrl+ zoom is pressed in both the top document and the frame, and the interceptor's own capture log is checked for any real token. Reverting the fix
reliably captures a real `jev:zoom-in|<token>` from BOTH the top document and the frame; with the fix, only the interceptor's own blind replay guesses
appear, none with a valid token, and neither closes the tab nor steals focus. Verified twice: once with a narrow standalone harness driven directly against
the app binary (to iterate quickly without the full 25-row suite), and once in the full official run below.

Building the row itself needed two rounds of fixing first, disclosed rather than smoothed over: a PowerShell quoting bug in the injected page markup (an
un-doubled single quote inside a generated script, which silently produced valid-but-wrong content rather than a hard error), and a click-target
size/position mismatch between the small iframe's CSS viewport and the whole app window's coordinates, which made an earlier attempt at this row pass for
the wrong reason (a coordinate click was missing its target, not because nothing could be captured). Both are fixed in `scripts/interaction-pass.ps1`.

## Local interaction pass (this machine, NOT clean-Windows evidence)
`scripts\interaction-pass.ps1` extracts the ZIP itself, into a fresh uniquely-named folder under its run directory (it no longer trusts a caller-supplied folder), then drives it with real key presses, the mouse wheel and UI Automation, using a fresh data folder and a local web server. Results, with the zip hash, build commit and WebView2 version: [json](0.1.0-alpha.7-interaction-pass.json). **On this ZIP: 20 rows PASS, 1 FAIL, 4 NOT TESTED.** Windows 11 Pro 10.0.26200, WebView2 Runtime 153.0.4234.48.

**The 1 FAIL (row 15, search engine) is Google's own automated-traffic page (`google.com/sorry/...`), not a JevBrowse defect**: this machine had run enough
real Google searches across today's repeated testing that Google itself started challenging the traffic. The search-engine *selection* mechanism this row
also checks (choosing Google, and the choice surviving a restart) is unaffected -- row 15b, immediately after, confirms the choice persisted correctly.
Not rerun to chase a clean pass on an external site's rate limiting, per the standing instruction to keep testing bounded.

Covers everything in the alpha.7 feature set (bookmarks including the real file picker, search engine, history, downloads with Show in folder selecting the
exact file, marking a site Sensitive, zoom including inside an iframe and via Ctrl+wheel, browser shortcuts with the page focused, the address bar after
Enter and while typing, a second copy on the same data folder) plus **row 24, a security negative check**: a page and an embedded frame call
`chrome.webview.postMessage` directly -- no key press, no token -- to try to close the tab, zoom the page, add a bookmark and move keyboard focus into the
address bar; none of it has any effect, and a genuine Ctrl+W immediately afterward still closes the tab as normal. **Row 25** is the new cross-origin
capture/replay check described just above.

**Two real defects found this pass, both fixed in this build:**
1. **The security fix below (an audit finding) initially broke every shortcut and zoom action, real ones included.** The token was being set on the lease
   *after* its script had already been registered with an empty one, so every `jev:key:*`/`jev:zoom-*` message -- genuine or forged -- was silently dropped.
   Caught immediately by re-running this same pass (rows 18, 18b, 18c, 21, 22, 23 all failed); fixed by setting the token before the script is registered,
   inside the same constructor call. This is exactly why a regression needs a build that can fail it: the first version of this fix could not have failed
   any test, because it broke the feature it was meant to secure.
2. The negative-check row (24) itself raced its own attack page on the first attempt and needed the attack's timer lengthened and reordered against the
   report-log reset; its first failure was a false alarm in the test, not the product (address-bar focus legitimately stays where Enter left it, which is
   ordinary browser behaviour, not something the attack caused -- the row now establishes real page focus with a click before judging the result).

**Not verified / limits:** row 19 (default browser: opening that dialog writes to the current user's registry), row 14d (the "already saved" message is
only in the status line, which UI Automation did not expose here -- an accessibility note), the Private-session rows through the UI (covered by the
automated engine check), and rows 3, 5, 7-10, 12, 13 of the checklist (real account, upload, permission prompt, clear data, Private restart, offline,
missing runtime). Row 24 (a page/frame directly sending an untokened message) is same-origin; row 25 (the more realistic threat -- a page trying to
intercept and replay a real message) genuinely is cross-origin, on a different port/origin, and shows the same result. The token mechanism itself does not
distinguish by origin -- it rejects any script that does not know the per-lease token, regardless of where it runs.
**Clean-Windows validation remains outstanding and must be done by a tester on their own machine.**

## The candidate: one exact, frozen artifact
**This alpha.7 candidate is frozen at the ZIP below. No further rebuilds without a real bug found and fixed.**

| | |
|---|---|
| ZIP file | `JevBrowse-0.1.0-alpha.7-win-x64.zip` |
| ZIP SHA-256 (verified fresh against the file, not recalled) | `d6a791fe7a898f1afe91b3cb175693a47986296bc235796548f930031989e65f` |
| `BUILD.json` inside that ZIP (read directly from the archive) | `{"commit":"4520ec2a74a9b74d481fb34cbdd4fe9edb3a006c","version":"0.1.0-alpha.7",...}` |
| Diagnostic logging (the engine-event log described below) | **NOT included.** Confirmed by diffing `MainWindow.AdditionsCheck.cs` at the ZIP's own commit (`4520ec2`) against the commit that added the diagnostic (`0d46b8e`, one commit later, `HEAD` as of this writing): the diagnostic code is absent at `4520ec2`. |

**Diagnostic-build results are not candidate results, and the distinction matters here specifically:** the three reproductions of the engine-check
failure that came from running the actual packaged smoke (`scripts\packaged-smoke.ps1 -Zip ...`, which extracts and hashes this exact ZIP each time) are
**candidate results** — the failure is confirmed to exist in the frozen artifact itself. The mutation test (reverting/restoring the postMessage fix), the
process-cleanup retry, and the diagnostic engine-event logging that ruled out a renderer crash were all run against a **separately built local binary**
(`artifacts\bin\JevBrowse.App\release_win-x64\`, rebuilt several times during the investigation, ending at a commit that includes the diagnostic code
this candidate ZIP does not have). Those are **diagnostic-build results**: they narrow down the cause and they are the reason this failure is confirmed
unrelated to the security fix, but they were not run against the exact bytes in the frozen ZIP. Nothing about this distinction weakens the finding that
the candidate ZIP itself fails the check — that part was confirmed on the candidate three separate times — but "the renderer didn't crash" and "it's not
the security fix" are diagnostic-build findings, one commit and one rebuild removed from the artifact being tested.

## Packaged smoke: a reproducible failure, root cause unresolved (engine checks / ClickDownload)
The packaged smoke's engine-checks step (`--additions-check`), run against **this exact candidate ZIP** via `scripts\packaged-smoke.ps1` (which extracts
and checksum-verifies it fresh each time), failed reproducibly -- 3 separate candidate runs, at the exact same point -- with
`System.ArgumentNullException: Value cannot be null. (Parameter 'json')`, inside a bench-only helper (`ClickDownload` in `MainWindow.AdditionsCheck.cs`)
that simulates a mouse click via DevTools. A further 4 reproductions were obtained on the diagnostic build described above (not the candidate ZIP itself)
while narrowing down the cause. **Root cause not found. This failure stays open** -- manual checklist passes below provide evidence for the individual
Private-session behaviors this bench normally covers, but none of them explain or resolve why the bench itself crashes, and this record does not treat
them as if they did. Marked unverified, not "does not affect users" -- that would be a claim this investigation did not earn. Full trace and reproduction
JSON: [0.1.0-alpha.7-packaged-smoke.json](0.1.0-alpha.7-packaged-smoke.json) (one of the three candidate-ZIP runs).

**Exactly which checks ran and which never got the chance to** (31 steps total in this bench; a crash mid-way does not mean everything after it is
"probably fine" -- it means those steps produced no result at all):

*Ran and passed (8):* a remembered permission decision is not asked about again; it is listed for its origin and profile; and not for another profile;
after Reset the next request is asked again; a Private session's in-memory permission decision is listed and resettable; both profiles have a cookie and
storage to begin with; clearing the Personal profile clears it; the Work profile is untouched.

*Never ran (23, in order):* a Private-session download shows a prompt before anything is saved · the prompt's wording and its Cancel default · dismissing
the prompt saves nothing · **Cancel on the prompt saves nothing** · **Continue lets the save proceed** · an ordinary tab downloads without being asked ·
a popup sign-in opens as a managed tab (recorded) · the sign-in page has no opener (recorded) · a Private session cannot add a bookmark · an ordinary tab
can be bookmarked · the bookmark is saved · bookmarking again removes it · the chosen search engine is used by the address bar · an ordinary page is in
the history list · **a Private-session page is not in the history list** · an ordinary download is in the downloads list · **a Private-session download
is listed in memory only, not written to the database** · Ctrl+ zooms the page · zoom is remembered per site · a fresh load keeps the remembered zoom ·
reset forgets the site's zoom · **zoom in a Private session is never written to disk** · **ending a Private session forgets its download's name (the file
stays)**.

Bold items have **no other automated coverage on this artifact**: the interaction pass explicitly defers all Private-session behaviour to this bench (see
its own "NOT TESTED: Private-session rows... covered by earlier automated checks" note) and does not re-test it through the real UI. The non-bold items in
the "never ran" list mostly duplicate something the interaction pass already confirmed through real clicks and keys (bookmarks, ordinary history, ordinary
downloads, zoom mechanics, search-engine choice) -- their absence here is a gap in this one bench's evidence, not a gap in the release's evidence overall.
The bold items are gaps in the release's evidence overall, and are exactly what [row 9 and row 16](../CLEAN_WINDOWS_CHECKLIST.md) of the clean-Windows
checklist now ask a tester to confirm by hand: Cancel and Continue exercised separately with their file outcomes checked, and the Private download gone
from the list (file intact) after the session ends.

**One targeted investigation was run (not a broad campaign), entirely on the diagnostic build described above, not the candidate ZIP, and it ruled out
the leading hypothesis rather than confirming it:**
- *Mutation test, both directions:* reverting today's postMessage fix reproduces the identical failure at the identical point; restoring it does not
  change the outcome. **Confirmed unrelated to that fix.**
- *Clean-environment retry:* stopping stray `msedgewebview2.exe`/test-server processes left over from a long session, then retrying, made no difference.
  **Confirmed unrelated to leftover process contention**, at least at the level this retry could detect.
- *Leading hypothesis, tested and ruled out:* that the Private tab's renderer crashed and was silently replaced mid-check (`WebView2LeaseManager`'s
  `ProcessFailed`/`EngineFailed` path), which would explain a null script result followed by a null `CoreWebView2`. A diagnostic was added -- logging
  every `engine-failed`, `restoring`, `restored` and `virtualized` kernel event to `<data>/benchmarks/additions-check-diagnostic.log` during this bench --
  and left in the source (harmless, append-only, `try`/`catch`-wrapped) rather than removed, so the next reproduction carries its own evidence instead of
  needing to be caught live again. One run showed the Private tab's renderer restoring normally (1398 ms) with **no** `engine-failed` or `virtualized`
  event anywhere near the failure. **This specific hypothesis is ruled out; the actual cause is still unknown.**
- *An attempted fix was tried and reverted, not shipped half-understood:* retrying the script call (since the tab's `Source` property updates before its
  DOM is actually ready) changed the symptom to a `NullReferenceException` on the WebView2 control shortly after, rather than resolving it -- a different
  failure, not a fix. The source tree was restored to exactly the committed state (`git status` clean, full unit suite re-run and passing) rather than
  keep a speculative change that traded one unexplained failure for another.

This was not pursued further, per the standing instruction to keep investigation targeted rather than run another broad campaign. The diagnostic log
stays in place for whoever picks this up next.

## Corrective pass (after an independent static review; four findings confirmed and fixed, one mismatch removed)
1. **One copy per data folder.** The single-instance guard used the program's folder while storage used `%LOCALAPPDATA%\JevBrowse`, so copies extracted to different folders could open the same database. Startup, storage and the crash log now share one resolver (`DataLocation`), the guard is keyed by it, and an exclusive lock file in the data folder is held for the life of the process. If a hand-over fails or the lock is taken, the copy stops with a message instead of carrying on. Verified with real processes (a copy started while another process held the folder shows the message and exits with code 1). The default-path equality is unit-tested only: no real run touched `%LOCALAPPDATA%`; the clean-Windows run uses it.
2. **History and zoom follow class changes.** Marking a site Sensitive (and a page turning Sensitive later, for example when a card field appears) now removes that site's saved history and zoom. A Private tab on the same site cannot erase an ordinary tab's history. Mutation-checked (removing the late purge, or the sweep, fails a test each).
3. **Download ownership.** A download's owner (container and workspace) is fixed when the page is created, not looked up when the download ends; an unknown owner is treated as Private (memory only); ending a Private session removes its download names from the list (the files stay). Verified in the engine check (31 steps).
4. **Release publishing.** A package built by one run is no longer what a second run publishes: publishing (`scripts\publish-release.ps1`, or the two-step workflow) refuses unless the zip's SHA-256 equals the hash you tested and matches `SHA256SUMS.txt`, and never attaches symbols. A tag push does nothing. **GitHub Actions is currently blocked by a billing/spending-limit notice on the account, so the workflow's package step could not be re-run and the corrected workflow is untested; the local script was exercised (matching hash passes, another hash is refused).**
5. Registration no longer claims `.htm`/`.html` file associations (the launch path only opens web addresses).

## New for developers
`scripts\dev.ps1 test|build|run|check`, a rewritten README and CONTRIBUTING (first change in ten minutes; how a change is checked), a feature-request template, and a two-step release workflow and `scripts\publish-release.ps1` (see the corrective pass).

## Verification (what was actually checked)
- Unit tests for each feature, including regressions that fail without the rule they protect. Mutation-checked: removing the scheme filter from bookmark import fails 2 tests (the first version of that test could not fail: it passed with the filter removed, and was strengthened); removing the class gate from the history recorder fails 2 tests.
- Real engine (`--additions-check`, 30 steps): a Private tab cannot bookmark; an ordinary page is in history and a Private page is not; an ordinary download is listed with its file present and a Private one is in memory only; zoom applies, is remembered per site, survives a reload and reset forgets it; Private zoom writes nothing.
- Two real processes (`external-link-check.ps1`, 6 checks): second start hands the link over and exits; a `javascript:` argument is ignored; a cold start with a link opens it.
- Default-browser registration: entries are current-user only; a real write/remove round-trip was run against a scratch registry key. **The real Windows default was not changed and the Settings hand-off was not clicked through.**

## Not verified / limits
- The new panels, the import file picker and the shortcuts were exercised through the same code paths, not by clicking each control; a clean-machine pass should click them.
- Zoom is page zoom applied by script (the WinUI web view does not expose the engine zoom): pages that size things in viewport units can differ.
- Not built: bookmark export, a bookmarks bar, download progress/cancel/retry, full-screen video (untested with the engine defaults), managed sign-in pop-ups (still unsupported).
- Still open and not code: signing certificate, update path, clean-Windows run, display scaling, high contrast, screen reader, the pixel-level no-flash screenshot observer.

## Data note
Database version is now **13** (bookmarks, history, downloads, site zoom). Data written by alpha.7 cannot be opened by alpha.4, alpha.5 or alpha.6: they refuse newer data and leave it untouched. Use separate data folders when switching.
