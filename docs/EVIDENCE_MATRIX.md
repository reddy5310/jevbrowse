# Evidence matrix

Every claim JevBrowse makes, and the strongest evidence behind it today. Levels, weakest to strongest:

- **Designed**: an ADR/doc says so; nothing verifies it.
- **Unit**: a test against fakes proves the logic (fast, but it cannot see engine or disk behaviour).
- **Real engine**: a benchmark/check drives actual WebView2 renderers and inspects real disk/DB state (`--privacy-check`, `--restore-bench`, …).
- **Release-validated**: verified on the packaged, signed build on more than one machine. **Nothing is at this level yet** (builds are unsigned and tested on one machine).

Last updated 2026-09-21 after P3, text size, agent isolation, screenshots, the agent indicator, the idle-CPU harness, the recovery work and the review response (ADR 0024-0032; [FIRST_RELEASE_PLAN.md](FIRST_RELEASE_PLAN.md)). 513 unit tests. See [RELEASE_CHECKLIST.md](RELEASE_CHECKLIST.md) for what is and is not done.

| Claim | Level | Evidence | Known gap |
|---|---|---|---|
| Disposing a renderer reclaims memory; suspend barely does | Real engine | ADR 0003, `--memory-lab`: 765 → 319 MB private (5 live → 1 live) | WebView2 process group only; the shell process is not in the number. One machine |
| Restore is fast | Real engine, thin | `--restore-bench`: p50 350 ms, p95 1.24 s | 8 samples, warm cache, measured to navigation-complete not "usable"; failed restores not counted |
| 50 tabs, ≤5 live; scheduler never thrashes | Unit | `TabKernelTests`, 2-hour thrash simulation, interleaving test (`PrivacyAndLifecycleTests`) | Simulation uses synthetic pressure; no low-RAM hardware run |
| Lifecycle operations cannot race (no duplicate renderers, no dispose-under-use) | Unit | 12 concurrent activations → 1 acquisition; 60 random interleaved ops keep tab state and renderer set consistent | Fake renderer latency, not WebView2's real async behaviour |
| **Private session leaves no durable trace** | **Real engine** | `--privacy-check` (real renderers): 0 thumbnails, tab rows, checkpoints, timeline entries, workspace rows, permission rows, index docs; private profile deleted on restart. Unit: `Switching_away_captures_a_thumbnail_only_when_policy_allows`, `Private_workspace_leaves_no_context_checkpoint…` | Crash dumps and OS-level artifacts (pagefile, DNS cache) are outside what we control and not checked |
| Sensitive pages store no pixels | Real engine | `--privacy-check`: real bank login page classified SECRET, 0 thumbnails, 0 checkpoint | One site; URL heuristics cover a fixed word list |
| A stricter class removes what the looser class stored | Unit | `Stricter_class_purges_thumbnail_checkpoint…`, `Tightening_a_pages_class_removes_it_from_the_index` | Not yet driven end-to-end in a real renderer |
| Identity boundaries are real (moving across identities creates a new tab; agents never touch personal identities) | Unit | `Moving_across_identities…`, `A_session_can_never_name_its_way_into_an_existing_identity…` | Cookie separation between profiles relies on WebView2 user-data folders (engine guarantee, not re-tested by us) |
| Permission grants are temporary and scoped to container + exact origin | Unit (key + policy) | `PermissionKeyTests` | `SavesInProfile=false` is set in the WebView2 adapter but not covered by an automated real-engine test |
| Ending a Private session closes its tabs and separately verifies profile deletion | Real engine + unit | [ADR 0019](adr/0019-private-session-end.md), [16-check probe](performance/private-session-check.json): cookies isolated, session reusable until ended, fresh identity afterward; late media and permission callbacks rejected. Unit: release failure, file locks, startup retry | One machine. Media and delayed permission answers injected; confirmation dialog and window shutdown not exercised by the probe |
| Page classification combines evidence by the stricter result | Unit | `Independent_evidence_combines_by_the_stricter_result` | — |
| **PUBLIC is earned, not assumed; "Not assessed" is honest** | Unit + real engine | `Public_must_be_earned_and_is_never_assumed`: the only routes to PUBLIC are a structurally public host or the user's per-site choice. No page-side signal promotes — absence of a sign-in affordance is not evidence of public availability | The host list is short and hand-maintained, so most sites sit at Not assessed until the user decides. That is the intended direction of error |
| **Nothing we could not assess leaves the device** | Unit | `A_page_we_could_not_assess_is_never_sent_even_on_an_explicit_request` covers both the chat and typed-decision routers with AI on, cloud on, providers configured and the user explicitly asking; `…MayLeaveDeviceOnExplicitRequest` is an enumerated set, not an ordinal test | Not driven through the real Ask dialog by an automated check |
| **Restore outcomes are distinguished and shown** | Unit | `CaptureResult` outcomes plus `PreservedParts` (Address/Position/Preview): `Shortfall` names the loss the user will notice and stays silent when only the preview was lost | The 12 s restore timeout and failure choices are not yet covered by an automated UI test |
| AI is off; background AI needs its own switch; nothing secret leaves | Unit | 21 Brain tests: kill switch, class gates, automatic-off, size cap, redaction of state *and* question text | Redaction is regex-based; `Redactor` recall on real pages is unmeasured |
| Cloud-call audit is accurate | Unit | task, class, automatic/explicit recorded; `CloudCallsByClass` counts typed Jev calls | UI shows the log; no export |
| Agents can only narrow a user-approved grant | Unit | `AgentCeiling` clamp tests; host fails closed without a ceiling; per-session approval | — |
| **An allowed URL cannot redirect an agent out of scope** | **Real engine** | `--agent-check`: real `youtu.be → www.youtube.com` 301 cancelled on the first load; policy registered before the renderer exists | One redirect shape; frame-level navigations not separately exercised |
| **Stop/expiry revoke and release, even protected pages** | Unit + real engine | `--agent-check` (0 live after Stop, `session_closed` after); `No_path_leaves_a_session_closed_with_live_pages` | — |
| Agent limits hold under concurrency | Unit | 8 concurrent navigations vs a 2-page limit; 20 concurrent requests vs a 5-action budget; in-flight request when Stop lands | Fake renderer latency, not WebView2 timing |
| Agent live-page quota is a hard limit | Unit | `Live_page_quota_is_a_hard_limit…` | CPU/network quotas do not exist |
| **Live capture blocks automatic hibernation, in frames and while a page is stalled** | **Real engine** | `--media-check`: WebRTC loopback negotiates and decodes frames (VP8/VP9/H264/AV1); real camera + microphone open through the permission path; the tab reports `MicrophoneActive, CameraActive`, the scheduler is refused **by those flags**, renderer and both tracks survive, and protection clears and sleeping resumes after stop. Frames served from `http://127.0.0.1`: a sibling and a frame nested two deep both capture; stopping one, destroying one, and **navigating one away** each leave the survivor protected, and a navigated frame stops claiming capture. A page blocked for 12 s keeps `MicrophoneActive` and is still refused — a missed heartbeat means *uncertain*, never *idle* | **Not exercised:** a real Meet/Zoom session end to end, the screen-share picker, disconnect/reconnect under memory pressure, and an unresponsive-renderer `ProcessFailed`. The `disconnected` recovery window, track-clone tracking and the `ProcessFailedKind` branch are implemented but covered by design only. `SharedArrayBuffer` was absent on a probe page that sends no COOP/COEP — **not** evidence that JevBrowse disables it |
| **20 heavy real sites open and close without a crash; the live-renderer cap holds** | **Real engine** | `--site-sweep` over `docs/sites.txt`: 20 tabs opened, never more than 4 live renderers, 16 scheduler evictions (= 20 - 4), closing everything took ~1.2 s and left 0 live renderers; the renderer process group then read 0 MB (that probe counts only WebView2 processes we started, so it shows they exited, not that the machine is otherwise unchanged). Found and fixed a real crash on the way: two permission prompts at once threw `Only a single ContentDialog can be open` inside an un-awaited dispatcher callback and killed the process; all 22 dialogs now queue through one gate and a prompt that cannot open denies that single request | 16 of 20 sites loaded; the other 4 are unreachable from this network (no DNS/connect), so they say nothing about the browser. Memory peaked at ~1.5 GB across 4 live renderers on heavy sites; no comparison with another browser was run. "ms to title" is when a title arrived, **not** page-load time. One machine, debug build |
| **The interface says what is true, in plain words** | Unit + screenshot | `KernelStatus` (pure): bookkeeping events produce no text, a first load is not announced as a wake-up, no message contains internal state names; `PrivateSessionPresentation` (ownership, background End, named Return, stale cleanup); `--ui-shot` in Simple and Power at 1422 px shows one toolbar, Explain and Receipt visible in Simple, correct workspace and tab counts | Narrow-window overflow, dialog and flyout layouts, and screen-reader behaviour of the new menus were **not** seen or tested. The screenshot method cannot capture WebView2 content or popups |
| **A keyboard or screen-reader user can reach and understand every control** | **Real window (UI Automation)** | `scripts/ui-a11y-check.ps1` launches the real app and exits 0 only when a full Tab cycle is proven and nothing failed, 1 on any unnamed/glyph-named control, missing required control or unreached control, 2 (never a pass) when the cycle is not proven, 3 on error; stops only its own process tree and never deletes anything (tested against a decoy second instance): 24 controls, 0 failures, full cycle, identical runs. Real screen captures of every dialog and menu (Tools menu no longer spills off-window; Shield/Receipt/Explain read in plain words) | Names present in the tree are necessary, not sufficient: **no real screen reader was run**, and no contrast, zoom, high-contrast or reduced-motion testing. Shift+Tab confirmed for six presses only (focus then left the app; not attributed to the browser). Needs an interactive desktop |
| Permission grants cover exactly one permission; dismissing a prompt never decides | Unit + real adapter | 9 unit tests (distinct kind per nameable permission; unnamed one never remembered; dismissal is refuse-once) and 3 checks in `--private-session-check` running the real `PermissionAdapter` against a real database | Old rows stored under `Other` are ignored, not purged. The dialog itself was not driven by a click in an automated check |
| **Text and controls are legible in dark and light; the app adds no idle rendering cost** | Unit (computed from the real files) + real window + measured | `DesignTokenTests` compute WCAG ratios from `App.xaml` and the welcome page CSS in both themes (text 7:1/4.5:1, boundaries/focus/graphics 3:1, badges and environment strip 4.5:1) and fail on raw colours or opacity-dimmed text; real captures of menus, dialogs and the welcome page in both themes; `scripts/idle-cost.ps1`, rule fixed in advance: after-P2 idle CPU 1.16-1.52% (mean 1.30) vs baseline mean 1.32, GPU 0.01%, trust ok ×3 | **High contrast not run on a real Windows contrast theme; scaling and the real Windows reduced-motion toggle not exercised.** First "after" set failed the instrument trust check on 2 of 3 runs (cause unknown, kept in `docs/performance`). Idle only: cost during transitions or with heavy pages not measured. One machine, debug build. 700 px address-bar clipping still open |
| Shield blocks ads/trackers on real sites | Real engine | `--shield-check` (goodreturns/cricbuzz/CNN), 110k rules, ~90 µs p95 | Debug build; long/adversarial URL cost unmeasured |
| Shield "disable for site" removes every layer | Unit + manual | per-renderer script ownership; disabled list baked into site modules | Needs a real-engine check that a disabled site really loads unmodified |
| YouTube ad definitions are pruned | Real engine | `--youtube-check`: Despacito 37 s of ads → 0 s | Ads are served non-deterministically; 3 videos; YouTube can change formats |
| Browser Memory ranks by relevance and stays bounded | Unit | BM25 sign fix test; budget test | Disk budget is text × 2, an estimate; real DB size is reported next to it |
| **Migrations cannot leave a database that cannot start, even if the process dies inside one** | Unit + real process kill | `A_failed_migration_step_rolls_back_completely…`; `CrashRecoveryTests`: a child process is KILLED with TerminateProcess inside step 8 and inside step 9 after their SQL ran and before commit: the file stays at the old version with data untouched, and the next start completes it (mutation-checked: running steps outside their transaction fails both) | Process death only: power loss or a failing disk (OS cache lost) is not simulated; WAL + `synchronous=NORMAL` may lose the last commits in a power cut |
| Filter-list activation survives a crash | Unit | `A_crash_between_the_two_activation_renames…` | Simulated, not a real kill |
| **A database from every earlier schema version upgrades without loss** | Real historic files + unit | `UpgradeFromHistoricDatabasesTests` (12): fixtures `tests/Fixtures/db/v1..v9.db` written by the real `BrowserDb.cs` of the commit that introduced each version (`scripts/make-db-fixtures.ps1`), seeded identically; every one upgrades to v9 with tabs, checkpoints, workspaces, permissions, FTS index and the once-only data-class renumbering intact; a newer-than-known database is left alone; **released migration steps are hash-pinned** so editing one fails a test (mutation-checked) | Fixtures are small (a handful of rows), not a real user's database; only schema versions this repository has shipped; nothing older than the first commit |
| **Interrupted writes and a killed process never leave partial or lost commits** | Real process kill | `CrashRecoveryTests`: 20 kills at random points during continuous 5-row transactions: `integrity_check` ok, no partial transaction, every acknowledged commit present (502-531 acknowledged before the last kill) | Process kill, not power loss; one machine, one filesystem |
| **A damaged browser.db cannot stop the app starting, and is never deleted** | Unit + real app | `DamagedDatabaseTests` (5): not-a-database and damaged pages are moved aside (pages intact) and a fresh database starts; a healthy or merely LOCKED database is never moved; two in one second do not collide; disabling the recovery fails 3 of the 5 (mutation-checked); `scripts/recovery-check.ps1 -Scenario corrupt-db`: real app starts on garbage, keeps the file, tells the person (dialog with OK), new database healthy | Found as a real gap: before this the app opened a window that never finished starting. A partly damaged large (>256 MB) database gets only the core-table check |
| **A crash or forced close with a live Private session leaves nothing readable outside its throwaway profile, and the next start sweeps it** | Real app (kill and normal close) | `scripts/recovery-check.ps1` crash + clean-shutdown (21 checks): a Private tab with a cookie, app killed with TerminateProcess: no Private URL/marker/cookie value in browser.db, its WAL or thumbnails; database copy passes `integrity_check`; the profile is left (a crash cannot clean up) and the next start removes it and its lock; normal tab survives; a normal close deletes the profile at once. Mutation-checked: disabling the start-up sweep fails 2 checks | Early "lingering engine processes" reports were a process-id-reuse bug in the check itself (fixed: same id and same start time; 12 clean runs since). Cookie VALUES inside the leftover profile are on disk until the next start (a crash cannot avoid that) |
| Cleanup retries while something holds a file | Unit | `Cleanup_retries_while_something_still_holds_a_file…`: retries then succeeds when released; held past the budget it reports failure, keeps the directory and a later sweep removes it | Windows file locking only |
| **Side panels, unified search, workspace previews and agent activity are usable by keyboard and stay inside the window** | Unit + real window with real keys | `ui-a11y-check.ps1` (Windows UI Automation + real keystrokes) for Shield, Explain, Receipt, Workspaces, Agent activity and Search: Enter opens, focus moves in, Tab keeps moving, Esc closes with focus back on the opening control, layout unclipped with each open; More menu lists palette, workspaces, agent activity, help. Passed at 700 and 1422 wide, sidebar shown and hidden. `UnifiedSearchTests` (7), `WorkspacePreviewTests` (6), `AgentActivityTests` (12) | Only the empty agent state and one live demo session were looked at; no real external agent (Claude Code) was driven through the panel. Scaling and text size untested. Gate is inconclusive, by design, while another window holds the foreground |
| **Nothing depends on Windows transparency effects** | Real window, real setting, pixels | `scripts/transparency-check.ps1` (only EnableTransparency; restored and read back): app confirms the setting; on vs off differ in 0.01% of pixels; title contrast 16.7:1 / 16.5:1 in both; gate PASS in both | Title-bar text only was measured for contrast; **real high-contrast theme not run** (needs a desktop-wide change; not authorised) |
| **The UI holds up at larger Windows Text size (125/150/225%)** | Real window, real keys, real setting | `scripts/text-size-check.ps1` changes only Text size (original recorded, restored and read back): gate at 1422 and 700 px, sidebar shown/hidden, plus permission and New-workspace dialogs, plus captures looked at. First run failed everywhere (toolbar buttons pushed off-window, tab list squeezed out at 225%); fixed with measured layout and a wrapping panel; final run 0 failures | **Display scaling (DPI) is untested.** At 225% the sidebar cuts workspace and tab names with an ellipsis. High contrast and reduced transparency not run |
| **An agent never moves the person's window** | Unit (with negative control) + real engine + real endpoint | `AgentDoesNotMoveTheWindowTests` (7; 3 fail against the old gateway); `--agent-window-check`: person's tab/workspace/page unchanged, agent page live and hidden, navigate and Read work; `--agent-check` scope and revocation still pass | **Screenshot returns no image in a Disposable session** (old and new behaviour alike). Heavy pages in a hidden view unmeasured. No toolbar indicator that an agent is working |
| **Background agent work never costs the person a page or their restore panel** | Unit (mutation-checked) + real engine | `BackgroundAdmissionTests` (10), gateway refusal tests, `--agent-window-check` with the person's restore concurrent with an agent navigate | The kernel does one thing at a time: a slow background acquire can delay a foreground click by that time |
| **Agent Screenshot is a separate, checked, memory-only operation that works on a hidden page** | Unit + real engine + geometry with positive control | `AgentScreenshotTests` (22): grant, budget, password refusal, size cap, cancel/expiry/late completion, HTTP base64, no files; real 20 KB PNG of a hidden page; staged control measured outside the window (on-canvas mutant fails) | **Pixel-level observation from outside was blind (black frames) and is unproven**; the earlier PASS is void. Ordinary thumbnails were not enabled |
| **Screenshot is experimental, opt-in per session, default off; password/payment fields in any frame refuse it; a picture is discarded after expiry or if the document changed** | Unit (each finding's regression failed first, and fails when its fix is reverted) + real engine with a two-origin local server | `AgentScreenshotTests` (22 methods), `PageSignalTrackerTests` (7), `--agent-frame-secret-check` (7 frame cases refused after the fix, all 7 delivered before; plain control and harmless iframe delivered), `--agent-show-during-capture-check` | **Pixel observation still unproven** (screen capture returned black; positive control not run); the no-flash claim stays unproven |
| **An agent working is visible on the toolbar and stoppable in one press** | Unit + real app via automation peers | `AgentIndicatorTests` (5), a structure test, `--agent-indicator-check` (present, named, opens the panel, Stop releases pages and hides it) | Keyboard reachability by the UIA gate not exercised (the gate runs without an agent) |
| **Idle cost stays about 1.4% of a core** | Measured, bisected | Baseline read ~7% after `d776ab2` (a finished restore left a visible indeterminate progress bar); fixed; 1.35-1.45% after; `--idle-invariants-check` + a mutation-checked source guard | Now measured by the harness below, report-only in CI; **not a gate**: the regression went unnoticed for about seven commits |
| **Idle CPU regressions are caught by a repeatable harness** | Measured, with positive control | `scripts/idle-benchmark.ps1`: warm-up, fixed sampling, shell vs WebView2 CPU, GPU separate, invalid runs inconclusive (28-check self-test, mutation-checked). Busy control 36% CPU / 5.2% GPU vs 1% static; the known regression (`RestoreProgress` left visible) reads 7-10% vs 1.1% and is flagged in all three scenarios; fixed vs fixed is not. Debug build, one machine; report-only in CI, enforcement needs a dedicated runner. GPU does not see this regression. `docs/performance/IDLE_CPU.md` |
| **Hidden agent pages are throttled and their cost is known** | Measured | `idle-cost.ps1 -AgentLoad`: hidden busy page ~3.0% CPU vs 39% shown (control); first agent page about +335 MB and +6 processes, then about +60 MB each | Debug build, one machine, synthetic pages; video/audio/WebRTC hidden unmeasured |
| **The address bar shows where the page in front really is; typing is never overwritten** | Real engine + unit | `--nav-check` (real WebView2, real address bar): link, Back, Forward, redirect, typing kept, abandoning the edit, background agent navigation leaves it alone (mutation: update disabled fails 3 steps); `AddressInput` (parse, 5000 random inputs never throw, only http/https/jev navigate); [result](performance/nav-check-2026-09-21.json) | Trusted clicks come from DevTools input events, not physical input |
| **The engine can never open its own window; a pop-up is a managed tab or is blocked** | Real engine (mutation-checked) + unit | `--nav-check`: a page-opened pop-up is blocked and reported, a clicked `target=_blank` becomes a tab in the opener's workspace and no top-level window is added; with the handler removed a real unmanaged window appears and the check fails. `PopupPolicy` (agent pages never, no gesture never, background page never) | OAuth pop-ups that need `window.opener` cannot work (opened without an opener) |
| **A crashed engine is replaced, not reused** | Real engine + unit | `--nav-check` kills the browser process: new renderer, page reloaded, address kept; `EngineFailureTests` (5) incl. protected tabs and stale reports | Kill of the browser process only; GPU-process failures are not treated as page death |
| **A site decision applies to that host only, and a stricter one acts immediately** | Unit (mutation-checked) | `SiteDecisionTests` (8): `co.uk`, `github.io`, subdomains; older two-label rows: stricter kept, Public ignored; Sensitive deletes preview, position picture and indexed pages at once | An old "Public" the person set earlier is dropped until they decide again |
| **Agent Read/Click/Type are rechecked after every wait; expiry and Stop are honoured** | Unit (mutation-checked) | `AgentAsyncWaitTests` (8), `AgentHostThreadingTests` (4, a single-thread pump; 2 fail on the old code); `recovery-check.ps1 -Scenario agent-close` on the real app | The confirmation dialog itself stays on screen after Stop until answered (its answer is discarded) |
| **A newer build's database is refused untouched** | Unit + real app | byte-for-byte unchanged, never quarantined; `recovery-check.ps1 -Scenario newer-db` shows the dialog and Quit | — |
| **Startup cannot be used half-initialised and failures are shown** | Real app + structure tests | `AppGuardStructureTests` (9); `newer-db` and `corrupt-db` scenarios; found and fixed a crash in the error path itself | Filter lists are fetched in the background on a first, offline run, so Shield has no rules until they arrive |
| **Remembered permission decisions can be seen and reset per origin and profile; the next request is asked again** | Unit + real adapter | `RememberedPermissionTests`, `--additions-check` (asked once, listed, not listed for another profile, Reset → asked again, Private in-memory grants resettable); the dialog says a running camera/microphone is not stopped | The dialog for an http(s) page is not driven by automation (the clear-data dialog is) |
| **Website data can be cleared for one identity profile only, with honest wording** | Real engine + real window | `--additions-check`: Personal cookie and localStorage cleared, Work untouched; `additions-check.ps1` drives the menu and dialog (profile named, sign-out warning, downloads and Browser Memory not deleted, Cancel is the default) | Per-site deletion is not available in WebView2, so the dialog offers the whole profile |
| **The last ordinary workspace and tab are resumed lazily; Private/Disposable/agent are never remembered** | Unit + real restart | `SessionResumeTests` (8), `additions-check.ps1`: a Private tab in front at close is not resumed, the preferences file holds ids only, only one tab awake after restart | Fallbacks (deleted tab or workspace) are unit-tested, not restarted |
| **A Private-session download is asked about first; Cancel saves nothing; agent pages never download** | Real engine (mutation-checked) | `--additions-check` shows the real prompt (text, Cancel default), dismissing it saves nothing, Continue saves, an ordinary tab is not asked | The engine's own save dialog is bypassed by the check; verify by hand (clean-Windows checklist row 9) |
| **Popup-based sign-in is NOT supported, and recorded as such** | Real engine | `--additions-check`: the sign-in page opens as a managed tab with `window.opener` null, so it cannot return a token; the status line says so | Deliberate for this alpha; a managed popup that preserves the opener is later work |
| **A missing WebView2 runtime gives a useful message** | Real app (simulated) | `additions-check.ps1` with the runtime folder pointed at an empty directory: message names the runtime, offers the download, Quit works | Simulated; a real machine without the runtime is the clean-Windows checklist row 13 |
| CI enforces performance regressions | **Designed** (measurement exists, enforcement does not) | `perf` and `idle-cpu` jobs publish numbers and never block; `idle-benchmark.ps1 -Enforce` and `idle-compare.ps1` exist | No stable dedicated runner; threshold not chosen from that runner's variation |
| Signed, updatable release | **Designed** | portable zip only | No code signing, no MSIX, no security-update path |

## Known limitations (deliberate, documented)
- **Most sites are Not assessed, and stay that way until you say otherwise.** PUBLIC comes only from a short list of structurally public hosts (Wikipedia, MDN, Microsoft Learn, …) or from your own per-site choice on the class badge. JevBrowse will not infer "public" from a page merely having content and no visible sign-out link — an authenticated report looks identical. The cost is that Browser Memory indexes little by default and Ask refuses on most pages until you classify the site. That is deliberate.
- **Product scope.** The architecture serves general users, developers, researchers and agents at once. The dependable first release is *durable workspaces with predictable memory-efficient tab restoration*; everything else is opt-in.
- **Not implemented:** bookmark import/export, extensions, password manager, sync.
- **Not verified:** a real screen-reader walkthrough (UI Automation names and keyboard paths are verified, which is not the same thing), real high-contrast themes, display scaling above 100%, signed/packaged install, upgrade and uninstall on a clean machine, meetings/uploads/long sessions/low-memory behaviour, Release-build performance, hidden pages playing audio/video/WebRTC.
